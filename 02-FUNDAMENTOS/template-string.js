const nombre = 'Baristo';
const real = 'Jordi';

const normal = nombre + ' '+ real;
const template = `${nombre} ${real} `
console.log(normal)
console.log(template)

const html = `
<H1>Hola</H1>
<p>Mundo</p>
`;

console.log(html);